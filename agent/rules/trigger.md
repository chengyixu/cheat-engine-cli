---
name: trigger
description: When an agent should use Cheat Engine CLI.
---

Use `cecli` when a task requires structured interaction with a process exposed by Cheat Engine `ceserver`, including discovery, module/thread/region inspection, memory reads or exact scans, remote administration, symbols, named pipes, bounded debug-event tracing, or an explicitly authorized mutation.
