---
name: writeback
description: How an agent records actionable CLI feedback.
---

Use `cecli issue create --type bug|requirement|suggestion|bad-output --message "..."`. Include the command, CLI version, endpoint class, exit code, and sanitized context. Never include credentials, private memory contents, or personal data.
