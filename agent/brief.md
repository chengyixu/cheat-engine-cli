Cheat Engine CLI (`cecli`) is a JSON-first, automation-safe command-line interface for the upstream Cheat Engine `ceserver` protocol. It discovers processes, modules, threads, regions, symbols, and remote files; reads and scans memory; performs confirmation-gated mutations; and supports bounded debugger events, breakpoints, and raw thread contexts for authorized debugging, interoperability testing, modding, and security research.
